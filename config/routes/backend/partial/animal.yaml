app_backend_partial_animal_latest:
    path: /latest/{count}
    methods: [GET]
    defaults:
        _controller: app.controller.animal::indexAction
        _sylius:
            repository:
                method: findLatest
                arguments: ['!!int $count']
            template: $template

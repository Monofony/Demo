{% import '@SyliusUi/Macro/headers.html.twig' as headers %}

<div class="ui stackable two column grid">
    <div class="column">
        {{ headers.default(header|trans, configuration.vars.icon|default('fax'), configuration.vars.subheader|default(null)|trans) }}

        {% include 'backend/booking/show/_breadcrumb.html.twig' %}
    </div>
    <div class="middle aligned column">
        {% include configuration.vars.templates.toolbar|default('backend/booking/show/_toolbar.html.twig') ignore missing %}
    </div>
</div>
